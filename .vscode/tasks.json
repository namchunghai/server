{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "lsof -i :5000 || (export WS_PORT=5000 && npm run start)",
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}
